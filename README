CareJS - The straightforward AJAX javascript framework

------
How to do a GET request:

<script type="text/javascript" src="care.js"></script>
<script type="text/javascript">
function foo(data, args) {
    // do stuff
}

care.xGet({
    url: 'http://example.com/somefile',
    vars: {
        arg1: 'foo',
        arg2: 'bar
    },
    handleAs: 'json',
    handler: foo,
    headers: {
        accept: 'application/xml'
    }
});

------
How to do a JSONP request

<script type="text/javascript" src="care.js"></script>
<script type="text/javascript">
function foo(data) {
    // the data-variable contains the returning JSON data
}

care.jsonp({
    url: 'http://example.com/somefile.php',
    vars: {
        arg1: 'foo',
        arg2: 'bar'
    },
    jsonp: 'callback',
    handler: foo,
    headers: {
        accept: 'application/xml'
    }
});
